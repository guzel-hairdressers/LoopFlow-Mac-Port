# Root Temporary Working Directory (`temp/`)

This directory is the primary location for temporary workspace projects, temporary issue scratchpads, transient proposals, and general scratch working files.

## Git Exclusion Policy

All contents within `temp/` (except `.gitkeep` and `README.md`) are ignored by Git via `.gitignore` to keep repository commits clean.
